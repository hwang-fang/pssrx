# 巨大なサンプルデータ（9.3GB）
/samples/
# Python 仮想環境
.venv/
__pycache__/
*.pyc
# Go
/go/bin/
# 解析出力
/out/
# ゴールデン出力（testdata/golden/*/intg/）は意図して追跡する。
# 生成元の Python 実装は退役済みで、これが期待値の唯一の記録になるため。

# Python 参照実装は独自の .git を持つサブリポジトリ。
# gitlink として取り込むとクローンが壊れるので親では追跡しない。
/sample_python/
station.yaml
