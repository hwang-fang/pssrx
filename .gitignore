# 巨大なサンプルデータ（9.3GB）
/samples/
# Python 仮想環境
.venv/
__pycache__/
*.pyc
# Go
/go/bin/
# 解析出力
/out/
# ゴールデン出力（go/testdata/golden/*/intg/）は意図して追跡する。
# tools/ を削除したあとはこれが期待値の唯一の記録になるため。

# Python 参照実装は独自の .git を持つサブリポジトリ。
# gitlink として取り込むとクローンが壊れるので親では追跡しない。
/sample_python/
