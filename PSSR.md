# PSSR 計算の手順と実装

測定局で受信した Mode A/C 応答から機体の位置を求める処理（`internal/pssr`）の
解説。何を計算しているか（原理）、どういう順に処理するか（手順）、コードの
どこがそれを担うか（実装）を、段ごとに書く。数値の根拠は実データ
（KX90 局、2026-06-10）で確かめたもの。

## 1. 何を計算するか

### 入力

| 入力 | 中身 | 出どころ |
| --- | --- | --- |
| 質問予定表 intg | SSR が各質問を**送信した時刻**と、そのときの**ビーム方位** | interrogator 段の出力 |
| 応答データ apkx | 測定局が各応答の F2 パルス（末尾）を**受信した時刻**、12 ビットの**応答符号**、波高値。読み込み時に F1–F2 間隔 20.3 µs を引いて先頭 F1 の時刻に直す | 測定局 |
| マスタ | SSR と測定局の位置（緯度経度・標高）、SSR の覆域、走査周期、PRI と質問種別 | `config` |

intg は「SSR がいつどちらを向いて質問したか」、apkx は「機体の返事がいつ局に
届いたか」を与える。SSR 自身の受信データは無い。

### 原理

トランスポンダは質問（P3 パルス）を受けてから公称 3.0 µs 後に応答を返す。
したがって応答 1 件が質問 1 件と対応づけば、

```
τ = t_受信 − t_送信 = 3.0 µs + (|SSR→機体| + |機体→局|) / c
```

から**双基地距離** `|SSR→機体| + |機体→局|` が決まる。この和が一定の面は
SSR と局を焦点とする回転楕円体である。さらに

- 質問に応答した以上、機体は**質問時のビーム方位**の面（SSR を通る鉛直面）にいる
- Mode C 応答が**気圧高度**を与える

ので、機体は「回転楕円体 ∩ 鉛直面 ∩ 高度面」の交点として 1 点に決まる。
これが単局の PSSR で、局が 1 つあれば成立する。

### 出力

1 機体 × 1 ドウェル（ビームが機体を照射した区間）につき 1 件の位置
（緯度・経度・標高）。スコーク、気圧高度、方位、τ、応答数を添える。

## 2. 手順の全体像

```
apkx ─┐
      ├─ Pair ──► プロット ──► Suppress ──► Locate ──► Sink
intg ─┘   (2.)      (3.)         (4.)        (5.)     (6.)
```

| 段 | 関数 | 入力 → 出力 | 持ち越す状態 |
| --- | --- | --- | --- |
| 時刻同期 | `pssr.Synchronizer` | 応答・質問予定を投入 → 処理できる範囲を取り出す | 待ち行列 2 本 |
| 対応づけ | `pssr.Pair` | 取り出した範囲 → プロット | `RunState`（開いている列） |
| 幽霊抑圧 | `pssr.Suppress` | プロット → プロット | `SuppressState` |
| 位置推定 | `pssr.Locate` | プロット → 位置 | なし（`Geometry` は不変の文脈） |
| 出力 | `pssr.Sink` | 位置 → CSV など | writer（SSR・局の ID は文脈として持つ） |

`pipeline` がブロックごとにこの順で呼ぶ（`pipeline.pssrStep`）。段は
互いを知らず、ループだけが順番を知る。ブロックは `record.Block` で、ファイル
からは `archive.FileSource` が 1 分 1 ファイルをそのまま 1 ブロックにする。件数の集計は呼び出し側が持つ
`pssr.Stats` に各段が足し込む。

ブロックをまたいで持ち越す記録は 3 つ。

- `Synchronizer`: 投入された質問予定と応答の待ち行列（時刻順）
- `RunState`: 開いている応答列と質問の通し番号
- `SuppressState`: 同じ走査の相手が出揃うまで判定を待つプロット

投入の刻み（1 分でも 0.1 秒でも）と対応づけの手続きは `Synchronizer` が
切り離す。

## 3. 対応づけ（`Synchronizer`, `sync.go` / `Pair`, `pair.go`）

投入 → 取り出し → マージ → 応答列 → プロット、の順。

### 3.1 時刻同期と取り出し（`Synchronizer`）

質問予定と応答は届き次第 `PushInterrogations` / `PushReplies` で投入する。`Extract` は
「取り出した範囲の外のデータが、範囲の中のデータと対になることはない」を
満たす先頭部分だけを切り出して内部から削除する。

```
TauMin = 3 µs + d / c                    d は SSR–局の基線長。これより短い経路は無い
TauMax = 3 µs + (2·R_max + d) / c        R_max は SSR の覆域。機体はその内側
```

質問 q への応答は `t_q + TauMin ≤ t_r ≤ t_q + TauMax` にある。したがって

- 応答の最新時刻 `T_r` に対し `t_q ≤ T_r − TauMax` の質問は応答が出揃っている。
  ここまでの質問を取り出す
- 最後に取り出した質問 `q_k` について `t_r ≤ t_q(k) + TauMax` の応答を一緒に
  取り出す。残る応答はどの取り出し済み質問からも `TauMax` 以上離れているので、
  将来の質問にしか属せない
- 取り出した応答が未知の次の質問 `q_{k+1}` に属することも無い。
  `t_r − TauMin ≤ t_q(k) + TauMax − TauMin < t_q(k) + PRI_min ≤ t_q(k+1)` で、
  `TauMax − TauMin < PRI_min` は `pipeline.PSSRParams` が検査し、覆域が広すぎる
  マスタは拒否する（KX90S: PRI 2.95 ms に対し TauMax − TauMin 2.67 ms）

過去には戻れない。取り出し済みの時刻より前のデータが後から投入されても
捨てる（`Stats.DroppedIntg` / `DroppedReplies`）。片側の投入が止まって他方が
溜まり続けないよう、保持幅 `MaxRetentionNs`（5 分）を超えた古いデータも捨てる。

### 3.2 マージで対にする（`Pair`）

取り出した範囲は閉じているので、質問列と応答列を時刻順にマージするだけで
対応が決まる。応答 r が属する質問は `t_q ≤ t_r − TauMin` を満たす最後の質問で、
`τ = t_r − t_q` が `TauMax` 以内なら対になる。範囲の最初の質問より前の応答と、
`TauMax` を超えた応答は対にならない（`Stats.Unpaired`）。

### 3.3 応答を列にまとめる（`assignToRun`）

同じ機体は 1 ドウェルの間に連続する十数の質問に応答するので、τ がほぼ
一定の応答列になる。開いている列のうち次の条件をすべて満たすものへ入れ、
無ければ新しい列を開く。

| 条件 | 既定値 | 根拠 |
| --- | --- | --- |
| τ の差（列の最後の応答との差）が `TauToleranceNs` 以内 | 1 µs | 応答遅延の公差 ±0.5 µs。実測のジッタは RMS 60 ns で、1 ドウェル内の機体の移動は 100 ns 未満 |
| 列がこの質問の応答をまだ持っていない | — | 1 つの質問に 1 機体の応答は 1 つ |
| Mode A 応答なら、符号が列の Mode A 符号と一致 | — | 同じ機体のスコークは変わらない |

途切れが `MaxGap`（2）以内かはここでは見ない。質問ごとに 3.4 で途切れの
大きい列を閉じてから次へ進むので、開いている列はすべて条件を満たしている。
複数の列が合えば τ の差が最小の列、同点なら生成の古い列をとる。

**走査の構造。** 応答 1 件の割り当てで開いている列を全部見るが、開いている
列は数十本（実測 平均 37、最大 93）で、τ の許容差を通るのは平均 0.05 本
しかない。閉じた列の 97% は応答 1 件だけの FRUIT で、割り当てのほとんどは
「合う列が無いので新しく開く」で終わる。そこで `RunState` は走査で読む
値（各列の最後の τ）だけを連続した配列 `tau` に置き、列の残り（応答、
最後の質問番号、Mode A 符号）は `cold` に分けて許容差を通ったときだけ
読む。数十本の τ は数百バイトでキャッシュに収まる。配列の並びは列を
開いた順で、閉じるときは順序を保って詰める。これで同点の選び方と、同じ
質問で閉じた列のプロットの順が、走査順そのものから決まる。`cold` の空き
slot は再利用するが、応答のスライスはプロットになった列では手放す
（`Plot.Replies` がそのまま持つ）。

**Mode C 符号は一致を求めない。** 上昇・降下中の機体は 100 ft の境界を
1 ドウェルの間にまたぐことがあり（3000 ft/min なら 1 ドウェルあたり 1.5%）、
符号一致を求めると正常な列が割れる。列には出現順の全符号を残し、高度は
3.5 で決める。

質問の通し番号は `RunState.seq` で取り出しの境界をまたいで数え続ける。

### 3.4 列を閉じてプロットにする（`closeRuns`, `makePlot`）

列は「もう応答が来ない」と分かった時点で閉じる。番号 `s` の質問の応答まで
消費し終えたら、最後の質問が `s − MaxGap − 1` 以前の列には加わる応答が
残っていない。取り出しの境界付近で終わった列は開けたまま次の範囲へ
持ち越し、処理の終わり（`CloseRuns`）で全部閉じる。

閉じた列のうち `MinReplies`（3）未満は FRUIT（他 SSR への応答）の偶然の
一致とみなして捨てる。残りを 1 プロットに要約する。

| 項目 | 定義 |
| --- | --- |
| 時刻 | 最初と最後の質問時刻の中点 |
| 方位 | 最初と最後の質問方位の中点（`midAngle`。2π の巻き戻りを考慮） |
| τ | 全応答の τ の平均（偶数丸め） |
| スコーク | 列の Mode A 符号を復号（3.5）。Mode A 応答の無い列は正常ではないので捨てる（`Stats.NoModeA`） |
| 気圧高度 | 列の Mode C 符号から決める（3.5） |

プロットは要約だけを持ち、生の応答符号は `Plot.Replies`（対応づいた応答列）
から復号し直せる。

方位を「最初と最後の中点」にするのは古典的な SSR の beam splitting と同じ
定義で、途切れがあっても偏らない。振幅で重み付けはしない。トランスポンダの
送信電力は一定で、局で受ける振幅は機体→局の経路で決まり、SSR のビーム
形状と無関係なため。

### 3.5 符号の復号（`decode.go`）

apkx の 12 ビット応答符号は、MSB から

```
D1 D2 D4 A1 A2 A4 B1 B2 B4 C1 C2 C4
```

の順に詰まっている。仕様書が無いため実データから決めた。Mode C 応答で
常に 0 のビット（D1 と、62,700 ft 以上でしか立たない D2）が bit 11, 10 に
あること、この配置で Gillham 復号が 99.7% の応答で妥当な高度になり、列の
中で 99.6% が連続すること（次点の配置は 92%）から一意に決まる。

- **Mode A**（`Squawk`）: A, B, C, D の各桁を `1·x1 + 2·x2 + 4·x4` で組み、
  8 進 4 桁 ABCD にする
- **Mode C**（`Altitude`）: Gillham 符号。500 ft 刻みは D2 D4 A1 A2 A4 B1 B2 B4
  のグレイ符号、100 ft 刻みは C1 C2 C4 のグレイ符号で、500 ft の段が奇数なら
  向きが反転する。D1 が立つ、C の値が 0/5/6 の符号は復号できない

列の高度は次の規則で 1 つに決める（`resolveAltitude`）。復号できない符号
（ガーブル）は除く。残りの最大と最小の差が 100 ft 以内なら正常な境界またぎ
とみなし、プロット時刻に最も近い応答の高度をとる。それより散っていれば
符号が壊れているので決めず、プロットにしない（`Stats.AltitudeSpread`）。
Mode C 応答が 1 つも無い列も同様（`NoAltitude`）。高度の無いプロットは
位置が解けないので、出力しない。

決まった高度が `Config.MaxAltitudeFt`（既定 60,000 ft）を超える列も捨てる
（`AltitudeTooHigh`）。Mode C の符号は 126,700 ft まで表せるが、実在の
機体はそこまで上がらない。応答が 3,850 件/秒に達する環境では、FRUIT が
τ 1 µs 以内に 3 つ揃う偶然が 2 時間で千件のオーダーで起き、その列の高度は
無作為なので、およそ半分がこの上限で落ちる。残りの半分は航跡の連続性で
落とすほかない。

### 3.6 ブロック境界

質問予定はドウェル対が閉じてから出るので、ブロック N の分はブロック N+1 で
届く。`Synchronizer` は応答が揃った質問だけを取り出すので、遅れは待ち行列が
吸収する。ファイル経由（`pssrx pssr`）とメモリ直列（`pssrx run`）は同じ
投入 → 取り出し → `Pair` の手順で、結果はバイト単位で一致する
（`TestRunMatchesFileMode`）。取り出した範囲は削除され、閉じた列は
捨てられるので、常駐運転でも待ち行列は増えない（`TestBufferIsBounded`）。

## 4. 幽霊抑圧（`Suppress`, `suppress.go`）

実データには、1 機が同じ走査に複数の方位で現れる幽霊プロットが実プロットと
同じ程度の数ある（深夜 10 分で 2589 プロット中 1406 件）。正体は 2 つで、
どちらも SSR で知られた現象。

| 種類 | 現象 | τ | 方位 | 列の長さ |
| --- | --- | --- | --- | --- |
| サイドローブ | 近い機体（十数 km）が SSR のサイドローブ質問にも応答する | 主ビームと同じ | 無関係 | 短いことが多い |
| 反射 | SSR のビームが反射体（山・建造物）に当たって遠くの機体を照射する | 主ビームより経路差ぶん大きい（6〜30 km ≈ 20〜100 µs） | 反射体の方向 | 主より長いこともある（17%） |

### 判定

同じ走査・同じスコーク・同じ高度のプロットを 1 機体の群とみなす。

- 同じ走査: 時刻差が走査周期 × `SameScanFraction`（0.75）以内
- 同じ高度: 復号高度の差が `AltitudeToleranceFt`（200）以内。同じ走査でも
  最大 2 秒ずれるので、上昇・降下中は 100 ft の境界をまたぐ

群の中で τ 最小のものから `DirectTauToleranceNs`（5 µs）以内を**直接照射の
候補**（主ビーム、サイドローブ、ガーブルで割れた列）とし、応答数が最多の
1 件を残す。それより τ の大きいものは**反射**として落とす。5 µs は同じ走査内の
機体の移動（2 秒 × 250 m/s ≈ 1.7 µs）を吸収し、反射の経路差（20 µs 以上）
とは十分離れる。

**方位は使わない。** 反射体の方向は機体と無関係で、方位差は 90° 以上が
大半（982 件中 783 件）。角度で絞ると反射が通ってしまう。

**Mode C 符号の一致でなく復号高度の差で見る。** 同じ機体でも時刻がずれれば
符号は隣の値に変わる。

同じスコーク・同じ高度・同じ走査に別の機体がいれば、τ の大きい方が落ちる。
稀だが起こりうる。航跡処理が入れば救える。

### 保留

判定には同じ走査の相手が出揃っている必要があるので、プロットを窓の幅 +
余裕（走査周期の 1/10。列が閉じる順は時刻順と数質問ぶんずれる）だけ
`SuppressState` に保留する。判定済みのプロットも、後続の判定の相手として
必要な間は残す。

## 5. 位置推定（`Locate`, `locate.go`）

### 幾何と閉形式解

SSR を原点にした ENU 座標系（`geodesy.ENUConverter`）で考える。この回転は
剛体変換なので、ENU 上のユークリッド距離は ECEF 上の真の直線距離と一致する。
応答局を R = (E_r, N_r, U_r)、機体を P = (ρ sinθ, ρ cosθ, z)（θ はビーム方位、
ρ は地上距離、z は高さ）とおくと、拘束は

```
L = |P| + |P − R|,   L = (τ − 3 µs) · c        双基地距離（回転楕円体）
E = ρ sinθ, N = ρ cosθ                          方位（鉛直面）
U = z                                           高さ
```

機体と同じ高さの水平面に SSR と局を射影し、`d₁ = |P|`、`d₂ = |P − R|` を

```
d₁² = ρ² + z²
d₂² = ρ² − 2pρ + B² + (z − U_r)²     p = E_r sinθ + N_r cosθ,  B = √(E_r² + N_r²)
```

と書いて `d₂ = L − d₁` を二乗すると ρ² が消え、`d₁ = ℓ + p̂ρ` という 1 次式になる
（`solveRho`）。

```
p̂ = p / L,   K̂ = 1 − p̂²
ℓ = ((L − B)(L + B) + U_r (2z − U_r)) / (2L)      実効半直弦
K̂ρ² − 2ℓp̂ρ + (z² − ℓ²) = 0
ρ = (p̂ℓ + √(ℓ² − K̂z²)) / K̂
```

局の高低差 `U_r` は `ℓ` の加法項として厳密に入り、分岐は無い。`(L − B)(L + B)` を
`L² − B²` と書くと機体が基線付近（L ≈ B）で桁落ちする。`B` は `Geometry` に
事前計算する。`p` は出力座標に要る `sinθ, cosθ` から作るので、三角関数は
`Sincos` 1 回で済む。

**解の一意性**は根の積 `(z² − ℓ²)/K̂` の符号で決まる。`|z| < ℓ` なら正根はちょうど
1 つで、和の根がそれになる（`K̂ ≤ 1` から判別式は自動的に正）。差の根は計算
しない。判定は `|z| < ℓ − ZMarginM`（200 m）の 1 行で、余裕は SSR 直上の除外
円錐を確実に避けるためのもの。棄却は次のとおり。

| 条件 | 扱い |
| --- | --- |
| L ≤ 0 | `Stats.Inconsistent` |
| L ≤ B + `BaselineMarginM`（500 m） | 基線特異点（`Baseline`）。z ≈ 0 で一意性判定が効かない場合の安全弁で、先に見る |
| `\|z\| ≥ ℓ − ZMarginM` かつ p̂ > 0 | 正根が 2 つ（`Ambiguous`）。機体が基線の近傍 |
| `\|z\| ≥ ℓ − ZMarginM` かつ p̂ ≤ 0 | 正根が無い（`NoSolution`）。与えた高さに解が無い |
| 曲率の反復が `CurvatureMaxIter`（5）回で収まらない | `NonConvergent` |

棄却領域は SSR 直上から局の方向へ基線に沿って伸びるレンズ状で、SSR 直上と
（`U_r ≈ 0` なら）局直上はどちらも `ℓ = z` の境界に厳密に乗る。両者は同じ L・
同じ射線で区別できない。KX90 は B = 1.3 km なので、この領域は空港の真上の
狭い範囲に限られる。

採用した解は `| d₁ + d₂ − L |` を検算して `Position.ResidualM` に、反復回数を
`Position.Iterations` に持たせる。

### 地球の曲率

400 km 先では地表が 12 km 以上落ちるので、z は ρ に依存する。「z を与えて ρ を
閉形式で解く → その ρ の水平位置で標高が h になる z を求める（`pointAt`）」を
z が `CurvatureTolM`（0.05 m）未満しか動かなくなるまで繰り返す。収縮率は 400 km
でも 1/500 程度で、2〜3 回で収まり、止めたときの z の誤差はその 1/500。z の更新に球近似 `ρ²/(2Rₑ)` は使わない。400 km では高次項が
12 m、Rₑ の選び方で 100 m 以上ずれ、それがそのまま出力標高に乗るため。
ENU ⇄ 緯度経度の変換と、標高（ジオイド面からの高さ）と楕円体高の差は
`geodesy` が JPGEO2024 ジオイドで厳密に扱う。大気屈折は無視する。

### 高度

Mode C の気圧高度は標準大気（1013.25 hPa）基準で、実際の気圧配置とは
数百 m 違いうる。いまは単位換算（ft → m）だけで標高として使う
（`HeightFromPressureAltitude`）。QNH と気温による補正はこの関数に集約して
後から足す。

### 誤差の伝播（`covariance`）

`F(ρ) = d₁ + d₂ − L = 0` の陰関数微分から

```
gf     = ρ/d₁ + (ρ − p)/d₂ = cos ε₁ + cos ξ₂
∂ρ/∂L = 1 / gf
∂ρ/∂z = −(z/d₁ + (z − U_r)/d₂) / gf
```

ヤコビアン `∂P/∂L = u ∂ρ/∂L`、`∂P/∂θ = ρ (cosθ, −sinθ, 0)`、
`∂P/∂z = (0,0,1) + u ∂ρ/∂z`（u = (sinθ, cosθ, 0)）で
`C = J diag(σ_L², σ_θ², σ_z²) Jᵀ` を ENU の 3×3 共分散として `Position.Cov` に
持たせ、CSV には σ_E, σ_N, σ_U を出す。σ の既定値（`Config`）:

| 量 | 既定値 | 根拠 |
| --- | --- | --- |
| σ_L | c · √(σ_timing² + σ_transponder²)、σ_timing = 100 ns、σ_transponder = 0.5 µs / √3 | intg の 100 ns 量子化と応答のジッタ（実測 RMS 60 ns）、応答遅延の公差 ±0.5 µs を一様分布とみなす |
| σ_θ | 0.1° | ビーム中心の方位 |
| σ_z | 30.48 m / √12 | Mode C の 100 ft 量子化 |

遠距離では `gf ≈ 2` なので距離方向は σ_L / 2 ≈ 50 m、方位に直交する方向は
ρ σ_θ（200 km で 350 m）で、**遠距離では方位誤差が支配的**。基線上では gf → 0 で
距離の誤差が発散するが、その領域は基線余裕と一意性判定が先に棄却する。

### 精度の目安

合成データ（既知の位置から τ と方位を作る `simtest.Observe`）では、近距離
から 380 km まで 1 m 以内で復元する（τ の 1 ns 量子化 = 0.3 m が支配的）。
実データでは航跡の速度の揺れ（±10%）から、位置のばらつきは数十 m 程度と
見える。

## 6. 出力（`Sink`, `sink.go`）

`Sink` は `Write([]Fix)` と `Close()` の 2 つを持つ。いまは CSV
（`CSVSink`）だけで、時系列 DB は後から別実装を足す。列は

```
time_jst, ssr, station, squawk, pressure_alt_ft, lat, lon, alt_m, azimuth_rad, tau_ns, replies,
sigma_e_m, sigma_n_m, sigma_u_m, track, status
```

緯度経度は小数 7 桁（約 1 cm）、標高は mm、時刻は JST の ns。`sigma_*` は
SSR の ENU 系での位置の標準偏差 [m]。`track` は便 ID、`status` は連続性の
判定（`ok` / `unconfirmed`）で、棄却した点も書く。

## 7. 設定と定数の置き場

| 種類 | 置き場 | 例 |
| --- | --- | --- |
| 局・SSR ごとに違う値 | マスタ YAML → `pssr.Params` | 基線長から導く TauMin/TauMax、走査周期、覆域 |
| 手続きの定数（局によらない） | `pssr.Config`（`DefaultConfig`） | τ の許容、途切れ、最小応答数、抑圧の閾値 |
| 物理定数 | `config` の定数 | 光速、応答遅延 3.0 µs |

設定の書式から `Params` を導くのは `pipeline.PSSRParams` の仕事で、`pssr` は
設定の書式を知らない。

## 8. ゴールデンと検証

- 段ごとの規則は `internal/pssr` の単体テストが合成データ（`simtest`）で
  固定する。対応づけ・途切れ・FRUIT・Mode C の変化・ブロック分割の同値性・
  抑圧の各ケース・復号の全高度往復・位置推定の復元精度
- `testdata/golden/rounding` に apkx 3 分を添え、intg + apkx から出した位置
  `fixes.csv`（303 件）とのバイト一致、メモリ直列とファイル再処理の一致、
  メモリ直列が書く intg とゴールデンの一致を確認する
- `fixes.csv` は現行実装の出力を固定したもの。仕様を
  意図して変えるときだけ `go test ./internal/pipeline -update-pssr-golden` で
  更新し、差分を記録する

## 9. 既知の限界と今後

- **FRUIT の短い列**: 交通量が多い時間帯（12 時台、毎秒 3400 応答）では、
  別 SSR に応答した遠方の機体の応答が 3〜5 個の列を作り、誤った位置になる。
  応答数 3 の位置の純度は 40%、8 で 90%、15 以上で 100%。`MinReplies` を
  上げると実機の短い列（ガーブルで割れたもの）も落ちる。航跡の連続性で
  判断する方が筋がよく、段 4〜5 間のフィルタ（Kalman など）の候補
- **同一スコーク・同一高度の別機**: 抑圧で τ の大きい方が落ちる
- **QNH・気温補正**: 未実装。`HeightFromPressureAltitude` に集約してある
- **大気屈折**: 未実装。n ≈ 1.0003 で 400 km なら 100 m 超。指数大気モデルで
  経路積分する案
- **多局統合**: 未実装。単局の求根 f(r) を局ごとに並べた最小二乗から始める
  合意
- **局間の時刻同期**: GPS 同期の前提。単局モードでも質問解析局と応答局は
  別々に指定する（intg に局の情報が残らないため）

## 10. 行わない最適化

列の集約（3.3〜3.4）は、開いている列の τ だけを連続配列に置く形
（`RunState` の `tau` / `cold`）にしてある。これは 2026-09-19 の計測で
決めたもので、その先の施策は実装して計測したうえで**採らない**と決めた。
条件が変わらない限り再実装しないこと。

計測条件: KX90、2026-06-10 10–12 時、応答 2,772 万件、1 スレッド。
開いている列は平均 37 本（最大 93）、閉じた列の 97.2% は応答 1 件だけ。

| 施策 | 内容 | 実測（壁時計 / `Pair`） | 判断 |
| --- | --- | --- | --- |
| （採用）τ の連続配列 | 走査が読む値だけを配列に置き、候補だけ本体を読む | 4.5 → 2.85 s / 3.14 → 1.66 s | 構造が単純で分布に依存しない |
| 孤立候補の分離 | 応答 1 件の列を別の待ち行列に置き、閉じるのを先頭を進めるだけにし、要約を通さない。昇格は墓標、順序は生成番号で保つ | 2.85 → 2.73 s / 1.66 → 1.59 s | **採らない。** 閉じる処理は 0.5 → 0.2 s に減るが、走査が残るので全体への効果が 4%。生成番号による順序の保証と墓標の分だけ読みにくくなる |
| 世代ごとの τ 順の窓 | 待ち行列を質問ごとに区切り、世代内の τ 昇順と質問内のクエリ τ の単調性を使って窓を前進だけで動かす | 2.73 → 2.60 s / 1.59 → 1.39 s | **採らない。** 走査は数回の比較になるが、残るのは応答 1 件あたりの定数（世代ループ、64 バイトの書き込み、分岐、統計）約 44 ns で、2,490 万件で 1.1 s。上の分離が前提で、複雑さに見合わない |
| τ の索引（バケット・二分探索・整列配列） | 開いている列を τ で引く | 未実装 | 開いている列は数十本で、連続配列の線形走査（1 本 1 ns 未満）に勝てない。対応づけの鍵が変わると索引も変わる |
| `PairedReply` の添字化 | 列の応答を値（48 B）ではなく入力への添字で持つ | 未実装 | `Pair` の呼び出しをまたぐ入力の所有権整理が要る。見込み 0.2 s |
| 応答のインライン配列 | 列の先頭 4 件を固定長で持ちスライスを省く | 未実装 | 割り当ては slot の再利用で既に 1 分あたり数百回。効果が無い |
| 並列化 | τ 軸や時間軸で分割 | 未実装 | 単一の SSR・局の組では逐次で足りる（1 分ブロック 19 ms）。多局化したら組ごとに並列にすればよい |

再検討の条件: 応答密度が 1 桁上がる（開いている列が数百本になる）、
または全日以上のバッチを頻繁に回す必要が出たとき。そのときは
「孤立候補の分離 + 世代窓」を合わせて入れれば `Pair` が 1.66 → 1.39 s
になることが分かっている（実装はブランチ `experiment/pair-solo-window`
の `b8e189c`、`d31f01e` にある。main からは外した）。
