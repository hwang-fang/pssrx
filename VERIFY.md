# 真値との突き合わせ

`pssrx` の位置（`pssrx run` / `pssr` の CSV）を、同じ測定局で受信した
ADS-B から作った真値と突き合わせて、便の純度・完全性、位置誤差、
連続性の判定の妥当性、エコーの候補を出す。

真値は `pssrx` の外で作る（spkx の生 Mode S フレームを pyModeS や rs1090 で
復号する）。検証の道具が被検証系と座標変換や時刻規約を共有しないためで、
`pssrx` が真値に求めるのは、ここに定める CSV の形だけ。

```
spkx ──(復号器: pyModeS / rs1090)──> 真値 CSV ─┐
                                               ├──> cmd/fixverify ──> 指標
qpkx + apkx ──(pssrx run)──> 位置 CSV ─────────┘
```

## 真値 CSV の形式

1 行が ADS-B の**空中位置メッセージ 1 つ**（DF17 / DF18、TC 9〜18 と 20〜22）。
再標本化はしない。`fixverify` が同じ機体の隣り合う行の間を内挿する。

```
time_jst,icao,squawk,lat,lon,pressure_alt_ft
2026-06-10T10:00:00.123456700,86d5c2,3113,34.7180211,135.2411932,3800
2026-06-10T10:00:00.612000000,86d5c2,3113,34.7192455,135.2398120,3825
2026-06-10T10:00:01.004000000,8695a1,,34.9012001,135.6013320,37000
```

| 列 | 型 | 内容 |
| --- | --- | --- |
| `time_jst` | `YYYY-MM-DDTHH:MM:SS.fffffffff` | メッセージの受信時刻。JST、タイムゾーン表記なし。小数部は省略可（あれば 9 桁まで）。`pssrx` の位置 CSV と同じ書式 |
| `icao` | 16 進 6 桁 | ICAO 24 ビットアドレス。大文字小文字は問わない |
| `squawk` | 8 進 4 桁、または空 | その時点でその機体が使っていたスコーク。下記の規則で決める |
| `lat` / `lon` | 10 進 [deg] | WGS84。小数 7 桁あれば十分（約 1 cm） |
| `pressure_alt_ft` | 整数 [ft]、または空 | メッセージの気圧高度（25 ft 分解能のものはそのまま）。復号できなければ空。空の行は位置として使い、高度差の集計からは除く |

- ヘッダ行は必須。列の順序は自由で、`fixverify` は名前で引く。知らない列は
  無視するので、復号器の都合で列を足してよい（後述の任意列を参照）
- 行は時刻順でなくてよい（読み込み時に整列する）
- 文字コードは UTF-8、改行は LF、引用符は不要
- ファイルは局・期間ごとに 1 つ。名前は自由（`fixverify -truth path` で渡す）

### `squawk` の決め方

スコークは Mode S の監視応答（DF5 / DF21、および Mode A への応答）から
分かり、位置メッセージには乗っていない。復号器は機体ごとに**最後に
受信したスコーク**を覚え、位置メッセージの行にはその値を書く。ただし
最後の受信から **60 秒**を超えていたら空にする（古い値を引きずらない）。

`pssrx` の位置はスコークでしか機体を識別できないので、`fixverify` の
対応づけの鍵はスコークと位置の近さ。スコークが空の行は位置の近さだけで
対応づけ、指標の集計では「スコーク不明」として分けて数える。

### 時刻

- 受信時刻は測定局の時計（GPS 同期）で、qpkx / apkx と同じ基準にする。
  spkx のタイムスタンプが分ファイルの先頭からの相対値なら、ファイル名の
  分を足して絶対時刻にする
- 位置メッセージの位置は送信時刻のもので、受信時刻との差は伝搬遅延
  （400 km で 1.3 ms）だけ。補正は不要
- CPR の全域復号は偶奇 2 メッセージから位置を出すので、出す行の時刻は
  **後の方（位置を確定させた方）のメッセージの受信時刻**にする。局所復号
  ならそのメッセージの受信時刻

### 出さないもの

- 地上位置（TC 5〜8）。滑走路上の機体は SSR の対象外
- 位置の無いメッセージ（識別、速度、状態）。ただし速度は任意列に使える
- CPR の復号に失敗した、または妥当性検査（前の位置からの移動が速度
  上限を超える、偶奇の不整合）に落ちたメッセージ。真値に誤りを混ぜるより
  行が無いほうがよい
- テスト送信・重複メッセージ（同じ内容が数十 ms 以内に再受信されたもの）

### 任意列

あれば `fixverify` が使う。無くてもよい。

| 列 | 内容 | 用途 |
| --- | --- | --- |
| `nic` | 位置の NIC（整数） | 精度の悪い位置（NIC < 7、半径 > 0.2 NM）を誤差の集計から除く |
| `gs_mps` / `track_deg` | 速度メッセージからの対地速度 [m/s] と針路 [deg]（最後に受信した値） | 内挿の精度向上、Kalman の評価 |
| `callsign` | 識別メッセージの便名 | 表示用 |
| `airborne` | `1` / `0` | 空中位置のみのはずだが、復号器が地上判定を持つなら書く。`0` は除外 |

## `fixverify` の使い方

```sh
go run ./cmd/fixverify -config station.yaml -ssr RJBB1 \
  -fixes out/fixes.csv -truth truth.csv -out out/matched.csv
```

`-config` / `-ssr` は誤差を距離方向・方位方向に分けるための原点（SSR の
位置）に使う。フラグ: `-gate-m`（門の固定分、既定 3 km）、`-sigma-az-deg`
（門に足す方位誤差の σ、既定 0.35°、3σ を足す。対応づけの門の話で、
`pssrx` の σ_θ とは別）、`-max-gap-s`（真値の内挿を
許す間隔、既定 10 s）、`-min-nic`（誤差の集計に使う NIC の下限、既定 7）、
`-list`（相手のいない確定便を何本並べるか）、`-smoothed`（位置誤差を
平滑化した位置 `sm_*` で集計し、速度 `vel_*` も真値の前後 5 s の差分と
比べる。対応づけは観測値のまま）。

真値 CSV を機体（`icao`）ごとの航跡にし、`pssrx` の各点の時刻に位置を
内挿する（隣り合う真値の間隔が `-max-gap-s` 以内のときだけ）。

対応づけは便単位。`pssrx` の便（`track` 列）の各点について、スコークが
一致し（真値のスコークが空なら位置だけ）、内挿した真値の位置との距離が
門（固定分 + 距離 × 3σ_θ）以内の機体を候補にし、最も近い機体を点の候補と
する。候補の最も多い機体について、**その機体の真値が内挿できた点**のうち
過半（かつ 3 点以上）が候補として指していればその機体を便の相手とする。
真値には受信の途切れがあるので、真値の無い点は不明であって不一致ではない。
相手と一致した点だけを誤差の集計に使う。

出す指標:

| 指標 | 意味 |
| --- | --- |
| `status` 別の真値あり率 | `ok` / `unconfirmed` / `echo` / `ambiguous` の点に候補がいる割合。連続性の判定と像の判定が正しかったか。像は反射体の方向に置かれるので `echo` に候補は居ないはず |
| 応答数別の真値あり率 | `MinReplies` の妥当性 |
| 便の相手と純度 | 相手のいる確定便の割合と、便の点のうち相手に対応した割合 |
| 完全性 | 相手のいる便の期間に、真値の機体が居た走査のうち点があった割合 |
| 位置誤差 | 距離方向・方位方向を別に、距離帯（50 km 刻み）ごとの符号付き中央値と絶対値の p50 / p90 / p99。高度差も。`-smoothed` なら平滑化した位置で |
| 速度（`-smoothed`） | 真値の前後 5 s の差分速度との差 \|Δv\| の p50 / p90 と、対地速度の差の中央値 |
| 相手のいない確定便 | エコーと ADS-B 未装備機の候補。同じスコークで時間の重なる相手ありの便との Δτ・Δ方位を並べる |

応答数別の方位誤差 [deg] と、出力の σ に対する残差 \|誤差\| / √(σ_E² + σ_N²)
も出す。σ の較正が実態に合っていれば、残差の分布は応答数によらず同じになる。

`-out` の CSV は `pssrx` の CSV に `icao`, `truth_e_m`, `truth_n_m`
（SSR 原点の ENU）, `truth_alt_ft`, `err_range_m`, `err_azimuth_m`, `matched`
を足したもの。方位誤差を角度で見る、応答数で層別するなどの追加の分析は
これから行う。

### 設定の検算にも使える

真値とスコーク・高度・時刻だけで対応づけた点の τ（双基地距離）から、
SSR の位置と方位の原点を最小二乗で推定できる。KX90 の暫定設定
（`testdata/kx90.yaml`）はこれで設定の位置が実際と大きく違うことを検出し、
局の座標の数百 m のずれも方位の原点の一定のずれとして現れた。設定が
正しければ、真値あり率と誤差がそのまま解析の評価になる。

### 実測（KX00 / RJBB1、2026-06-10 10–12 時）

| 指標 | 値 |
| --- | --- |
| `ok` の点の真値あり率 | 58%（残りは ADS-B 未装備機の便が主。τ の時系列でも真値に該当が無く、上昇プロファイルが一貫した実機） |
| `unconfirmed` の点の真値あり率 | 5% |
| `echo` の点の真値あり率 | 3%（KX90 では 5%）。真値の位置と一致する点で `echo` になったのは 0.5%（KX90 で 0.9%）で、空港の地上・離陸直後（τ ≈ 15 µs）の方位が定まらない重複 |
| `ambiguous` の点 | 509 点、真値あり 59%（KX90 では 990 点、37%）。先に始まって先に終わる組で、真値で見てもどちらが実機か半々 |
| 相手のいる確定便 | 1,203 / 2,338 本。純度 中央値 1.000、p10 1.000 |
| 完全性 | 98.6% |
| 同じ走査に直接波と幽霊が共存する対 | 2 時間で 4 組（高度一致は 0）。`Suppress` がほぼ処理している |
| 距離方向の誤差 | 中央値 +15〜+43 m、\|p90\| 136〜209 m |
| 方位方向の誤差 | 応答 14 件以上で \|p50\| 0.11°、\|p90\| 0.33°。応答 3 件では \|p50\| 1.6°、\|p90\| 2.3°。短い列はドウェルの断片で、中点がビームの片側に寄る。σ_θ を応答数で決める根拠（PSSR.md 5 節） |
| σ に対する残差 \|誤差\| / σ_h | 応答 3〜8 件で p90 ≈ 1.0（保守的）、14 件以上で p90 ≈ 2〜3（裾が重い）。基準 0.20° は p90 が 1.64σ に近くなる値 |
| 高度差 | 中央値 0 ft、\|p90\| 50 ft（Mode C 100 ft と ADS-B 25 ft の量子化） |
| 平滑化した位置（`-smoothed`） | 方位方向 50〜100 km \|p50\| 905 → 141 m、\|p90\| 2,297 → 522 m。応答 3 件の方位誤差 p90 2.29° → 0.59°。速度 \|Δv\| p50 3.6 m/s、p90 13.6 m/s。距離方向の中央値は +43 → +68 m に見えるが、観測値の方位誤差が動径への射影を内側に寄せていたぶんが消えただけ（PSSR.md 9 節） |

## 注意

- ADS-B 未装備機（Mode A/C のみ）は真値が無い。相手のいない便がすべて
  誤りとは限らない
- ADS-B 受信の覆域は SSR の覆域と違う（アンテナ・感度による）。完全性は
  ADS-B が受かっている範囲でしか測れない
- 真値の位置も誤差を持つ（NIC で規定される。NIC 8 で 93 m）。`pssrx` の
  誤差が数十 m の水準に近づいたら、真値の誤差を差し引いて考える
